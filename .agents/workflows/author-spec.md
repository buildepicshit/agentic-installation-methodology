# Author Spec

Single source: `/author-spec` is served by the `author-spec` skill
(`agents/skills/author-spec/SKILL.md`), which owns the mechanics —
including the refuse-unless pre-conditions (IDEA at
`status: ready-for-spec`) and the owner-only `approved` rule.
Shared schema: `agents/specs/SPEC.schema.md`.
