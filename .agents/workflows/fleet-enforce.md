# Fleet Enforce Workflow

(bes-fleet-policy only.) Single source: the `/fleet-enforce`
slash-command (`.claude/commands/fleet-enforce.md`) is the invocation
surface; the `fleet-enforce` skill
(`agents/skills/fleet-enforce/SKILL.md`) owns the directive
lifecycle, immutability, and failed-target rules. Directive format:
`agents/fleet-directives/README.md`.
