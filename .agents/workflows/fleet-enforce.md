# Fleet Enforce Workflow

(bes-fleet-policy only.) Single source: `/fleet-enforce` is served by the `fleet-enforce` skill
(`agents/skills/fleet-enforce/SKILL.md`), which owns the directive
lifecycle, immutability, and failed-target rules. Directive format:
`agents/fleet-directives/README.md`.
